{
  "name": "contracts",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "contracts/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/contracts",
        "main": "contracts/src/index.ts",
        "tsConfig": "contracts/tsconfig.lib.json",
        "assets": ["contracts/*.md"]
      }
    }
  }
}
